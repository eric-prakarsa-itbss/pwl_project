Buatlah sebuah Migrasi, Model, Controller dan Form untuk Table berikut :
1. Migrasi Table krs Isi dari Table
	id()
	unsignedBigInteger('kode_mahasiswa')
	string('tahun_ajaran')
	enum('semester', ['ganjil', 'genap'])
	enum('status', ['pending', 'approved', 'partial', 'declined'])
	integer('total_sks')
	timestamps()

2. Migrasi Table krs_detail Isi dari Table
	id()
	unsignedBigInteger('krs_id')
	unsignedBigInteger('kelas_id')
	enum('status', ['pending', 'approved', 'declined'])
	timestamps()
	
2. Model dan Controller untuk Tabel di atas
3. Form create
4. Halaman index & Halaman View / Detail
5. TIDAK PERLU MEMBUAT FORM EDIT